package com.education.tutoring.management.repository.jpa.shared.parser.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.data.jpa.repository.config.EnableJpaAuditing;

/**
 * {@code JpaPersistenceConfiguration} is a Spring application configuration class used
 * for configuring access to a relational DB instance
 */
@Configuration
@EnableJpaAuditing
public class JpaPersistenceConfiguration {

}
